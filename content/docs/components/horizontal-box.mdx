---
title: Horizontal Box
description: A horizontal flexbox layout component with optional reverse direction
---

import { ComponentPreview } from '@/components/component-preview';
import { HorizontalBoxBasic, HorizontalBoxReversed } from '@/components/examples/layout-examples';

## Preview

### Basic

<ComponentPreview
  preview={<HorizontalBoxBasic />}
  code={`import { HorizontalBox } from '@/components/ui/layouts/horizontal-box';

export default function Example() {
  return (
    <HorizontalBox>
      <div className="h-20 w-full bg-primary/20 rounded flex items-center justify-center">First</div>
      <div className="h-20 w-full bg-primary/20 rounded flex items-center justify-center">Second</div>
      <div className="h-20 w-full bg-primary/20 rounded flex items-center justify-center">Third</div>
    </HorizontalBox>
  );
}`}
/>

### Reversed

<ComponentPreview
  preview={<HorizontalBoxReversed />}
  code={`import { HorizontalBox } from '@/components/ui/layouts/horizontal-box';

export default function Example() {
  return (
    <HorizontalBox reversed>
      <div className="h-20 w-full bg-primary/20 rounded flex items-center justify-center">First (right)</div>
      <div className="h-20 w-full bg-primary/20 rounded flex items-center justify-center">Second (middle)</div>
      <div className="h-20 w-full bg-primary/20 rounded flex items-center justify-center">Third (left)</div>
    </HorizontalBox>
  );
}`}
/>

## Installation

```bash
npx shadcn@latest add @shoscn/horizontal-box
```

## Usage

```tsx
import { HorizontalBox } from '@/components/ui/layouts/horizontal-box';

export default function MyComponent() {
  return (
    <HorizontalBox>
      <div>Left</div>
      <div>Right</div>
    </HorizontalBox>
  );
}
```

## API Reference

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `children` | `React.ReactNode` | - | Child elements to display in the horizontal layout |
| `reversed` | `boolean` | `false` | Reverse the order of children (right to left) |
